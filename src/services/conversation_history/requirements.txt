# Python 3.11+
# Core dependencies
fastapi>=0.100.0
uvicorn>=0.23.0
loguru>=0.7.0
pydantic>=2.0.0

# HTTP client
aiohttp>=3.9.0

# Database (SQLite built-in, Redis optional)
redis>=5.0.0

# Security (for password hashing if needed)
cryptography>=41.0.0

